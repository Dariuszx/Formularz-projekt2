<?php

    $tablica = array( "Dariusz", "Kacper", "Patrycja", "Konrad", "Norbert" );

    $q = $_REQUEST['str'];


    echo "działa";
/*
$hint = "";

    if( $q !== "" ) {
        $q = strtolower( $q );
        $len = strlen( $q );

        foreach( $tablica as $name ) {
            if( stristr( $q, substr( $name, 0, $len ) ) ) {
                if( $hint === "" ){
                    $hint = $name;
                } else {
                    $hint .= ", $name";
                }
            }
        }
    }
*/

?>